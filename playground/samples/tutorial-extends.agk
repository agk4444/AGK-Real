class Animal:
    name as String
    constructor with n as String:
        set name to n

class Dog extends Animal:
    to speak and returns String:
        return "woof"

to main:
    d is Dog("rex")
    say d.name
    say d.speak()

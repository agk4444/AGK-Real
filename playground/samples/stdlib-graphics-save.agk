import graphics

import fileutils

to main:
    c is new(32, 24, "#0d1117")
    circle(c, 16, 12, 8, "#e94560", true)
    rect(c, 0, 0, 32, 3, [255, 255, 255], true)
    say save_png(c, "badge.png")
    say file_exists("badge.png")

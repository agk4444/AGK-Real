import pathutils

to main:
    say join_path("docs", "guide.md")
    say base_name("/tmp/x/report.csv")
    say file_extension("archive.tar.gz")
    say file_stem("/tmp/x/report.csv")
    say norm_path("a//b/./c")

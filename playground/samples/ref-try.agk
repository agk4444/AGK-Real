to risky with n as Integer:
    if n < 0:
        raise "negative!"
    return 10 / n

to main:
    try:
        say risky(- 1)
    catch err:
        say "caught: {err}"
    finally:
        say "cleanup runs"

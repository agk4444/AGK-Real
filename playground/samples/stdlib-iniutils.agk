import fileutils

import iniutils

to main:
    write_text("app.ini", "[server]\nhost = example.com\nport = 8080\n")
    say ini_get("app.ini", "server", "host")
    say ini_has("app.ini", "server", "port")
    say ini_sections("app.ini")

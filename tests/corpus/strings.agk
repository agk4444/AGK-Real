define function main:
    create name as String
    set name to "agk"
    print("hello, " + name)
    create flag as Boolean
    set flag to true
    if flag and not false:
        print("logic works")
